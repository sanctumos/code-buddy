# GitHub Webhook Processor - Environment Configuration
# Copy this file to .env and update values as needed

# Webhook Configuration
WEBHOOK_SECRET=your_webhook_secret_here

# Flask Configuration
FLASK_SECRET_KEY=your-flask-secret-key-here
PORT=5000
DEBUG=False

# Logging Configuration
LOG_LEVEL=INFO

# Letta Integration
# Used by both webhook_processor.py and mcp_server.py
LETTA_BASE_URL=https://api.letta.com
LETTA_AGENT_ID=your-agent-id
LETTA_TOKEN=your-letta-token
LETTA_PROJECT=your-project-id
LETTA_IDENTITY_ID=code_buddy

# MCP Server Configuration (STDIO mode - no ports needed)
# The MCP server uses stdin/stdout for communication (no network ports)

